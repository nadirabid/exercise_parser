version: 0.1
frontend:
  phases:
    build:
      commands:
        - cd web
        - npm install
        - npm run build
  artifacts:
    baseDirectory: web/build
    files:
      - '**/*'
  cache:
    paths: []
