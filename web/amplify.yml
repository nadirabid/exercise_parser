version: 0.1
frontend:
  phases:
    build:
      commands:
        - cd web
        - npm install
        - npm run build
        - cp -r src/.well-known build/.well-known
  artifacts:
    baseDirectory: web/build
    files:
      - '**/*'
