version: 0.1
frontend:
  phases:
    build:
      commands:
        - cd web/main
        - npm install
        - npm run build
  artifacts:
    baseDirectory: web/main/build
    files:
      - '**/*'
